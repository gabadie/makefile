#
# Travis configuration file (travis-ci.org)
# Generated by Candy Makefile (make travis/setup)
#

language: c

# compilers wanted
compiler:
  - gcc

# command to install dependencies
#   Candy Makefile uses python and py.test
install:
  - "sudo apt-get install python"
#  - "sudo apt-get install python-pip"
#  - "sudo pip install pytest"
#  - "sudo pip install -r python_requirements.txt"

# command to run tests
script: "make hook"
